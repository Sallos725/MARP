services:
  risu-multiagent:
    build: .
    ports:
      - "6009:6009"
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      - CONFIG_PATH=/data/config.json
    restart: unless-stopped
